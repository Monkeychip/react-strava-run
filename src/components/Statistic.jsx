const Statistic = (props) => {
  return (
    <div className="flex justify-center items-center">
      <p className="text-slate-500 mt-5 mb-5">{props.text}</p>
    </div>
  );
};

export default Statistic;
